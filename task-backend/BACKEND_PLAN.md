# 📝 Backend Implementation Plan & Checklist

เอกสารนี้รวบรวมรายการสิ่งที่ต้องทำสำหรับ Backend เพื่อให้ระบบ Task Management สมบูรณ์และเชื่อมต่อกับ Frontend ได้ครบถ้วน

---

## 🏗️ 1. Database Setup (จัดการฐานข้อมูล)
ก่อนจะเขียนโค้ด Backend ต้องมั่นใจว่าใน Database มีตารางครบแล้ว

- [x] **Create Tables (สร้างตาราง)**: มีตารางครบแล้ว (Users, Projects, Tasks, Project Members)

    > **Note**: ข้อมูลตารางถือว่าพร้อมใช้งานแล้ว ดำเนินการขั้นตอนต่อไปได้เลย

---

## 🚀 2. API Development (พัฒนาระบบหลังบ้าน)

### 📌 Phase 1: Project Management (ProjectDetails Page)
*เป้าหมาย: เพื่อให้หน้า Project Details แสดงข้อมูลจริงได้*

- [x] **GET Project Detail** (`GET /api/projects/:id`)
    - ดึงข้อมูล Project ตาม ID
    - **สิ่งที่ต้องทำเพิ่ม**: เขียน Query Join เพื่อดึงจำนวน Task และสมาชิกในทีมมาแสดงด้วย

- [x] **Update Project** (`PUT /api/projects/:id`)
    - แก้ไขชื่อ, รายละเอียด, สถานะโปรเจกต์

- [x] **Get Project Members** (`GET /api/projects/:id/members`)
    - ดึงรายชื่อคนในโปรเจกต์นั้นๆ

### 📌 Phase 2: Task Management (Tasks & Kanban)
*เป้าหมาย: เพื่อให้สร้าง Task และย้ายงานใน Kanban ได้*

- [ ] **Create Task** (`POST /api/tasks`)
    - รับค่า: title, description, project_id, assignee_id, priority, due_date

- [ ] **Get Tasks by Project** (`GET /api/projects/:id/tasks`)
    - ดึง Task ทั้งหมดของโปรเจกต์นั้นออกมา

- [ ] **Update Task** (`PUT /api/tasks/:id`)
    - ใช้สำหรับเปลี่ยน Status (Drag & Drop ใน Kanban) หรือแก้รายละเอียดงาน

- [ ] **Delete Task** (`DELETE /api/tasks/:id`)

### 📌 Phase 3: Team Management
*เป้าหมาย: เพิ่มคนเข้าโปรเจกต์ได้*

- [x] **Add Member** (`POST /api/projects/:id/members`)
    - รับค่า: project_id, user_id
    - บันทึกลงตาราง `project_members`

- [x] **Remove Member** (`DELETE /api/projects/:id/members/:userId`)

---

## 🔌 3. Frontend Integration Checklist
จุดที่ Frontend ต้องกลับมาแก้หลังจาก Backend เสร็จ

- [ ] **ProjectDetails.jsx**: เปลี่ยนข้อมูล Mock Data เป็น `fetch` จาก `/api/projects/:id`
- [ ] **ProjectDetails.jsx**: เปลี่ยนรายการ Task เป็น `fetch` จาก `/api/projects/:id/tasks`
- [ ] **CreateTaskModal.jsx**: เชื่อมปุ่ม Submit กับ `/api/tasks`
- [ ] **AddMemberModal.jsx**: ดึงรายชื่อ User ทั้งหมดมาให้เลือก และเชื่อมปุ่ม Add กับ `/api/projects/:id/members`

---

## 💡 คำแนะนำในการเริ่ม
แนะนำให้เริ่มทำทีละคู่ (Backend Endpoint -> Frontend Integration) จะได้เห็นผลลัพธ์ทันที:
1. **เริ่มที่**: สร้างตาราง Database ให้ครบก่อน
2. **ต่อด้วย**: ทำ API `GET /api/projects/:id` และไปแก้ Frontend ให้ดึงข้อมูลมาโชว์
3. **จากนั้น**: ทำ API `POST /api/tasks` และไปแก้ Modal สร้าง Task
