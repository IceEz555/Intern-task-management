# 📘 Code Explanation: Project & Task Management System

เอกสารนี้อธิบายการทำงานของโค้ดส่วนที่เราเพิ่งพัฒนากันมาอย่างละเอียด (ช่วง Project Details & Task Creation) เพื่อให้เข้าใจหลักการทำงานแบบ Deep Dive ครับ

---

## 1. 🌐 ภาพรวมการทำงาน (Architecture)
ระบบทำงานแบบ **Client-Server** โดยมี:
*   **Frontend (React)**: ทำหน้าที่แสดงผลและรับ input จากผู้ใช้
*   **Backend (Express + PostgreSQL)**: ทำหน้าที่รับคำสั่ง, ประมวลผล และบันทึกข้อมูลลงฐานข้อมูล

---

## 2. 🖥️ Backend (หลังบ้าน)

### 2.1 Project Controller (`src/controllers/projectController.js`)
ไฟล์นี้คือ "สมอง" ของการจัดการโปรเจกต์ ฟังก์ชันสำคัญที่เราเพิ่มคือ `getProjectById`

**การทำงานของ `getProjectById`:**
1.  **รับ ID**: รับ `id` จาก URL (เช่น `/api/projects/6`) มาเก็บไว้ในตัวแปร
2.  **Query 1 (Project Details)**:
    *   ดึงข้อมูลหลักของโปรเจกต์ (ชื่อ, รายละเอียด, สถานะ)
    *   มีการใช้ **Subquery** เพื่อดึงจำนวน Task ทั้งหมด (`task_count`) และ Task ที่เสร็จแล้ว (`done_task_count`) ในคำสั่งเดียว เพื่อลดจำนวนรอบในการคุยกับ Database
3.  **Query 2 (Tasks)**:
    *   ดึงรายการงาน (Tasks) ทั้งหมดของโปรเจกต์นี้
    *   ใช้ **LEFT JOIN** กับตาราง `users` เพื่อดึงชื่อและรูป (`avatar`) ของคนรับผิดชอบงาน (`assignee`) มาพร้อมกันเลย
4.  **Query 3 (Members) - *จุดที่เคยมีปัญหา***:
    *   พยายามดึงรายชื่อสมาชิกทีมจากตาราง `project_members`
    *   เราใส่ `try-catch` ครอบไว้ เพราะถ้ายังไม่ได้สร้างตารางนี้ใน Database ระบบจะไม่พัง (Error 500) แต่จะคืนค่าเป็นอาเรย์ว่าง `[]` แทน ทำให้หน้าเว็บยังทำงานต่อได้
5.  **Response**: รวมข้อมูลทั้ง 3 ส่วนส่งกลับไปให้ Frontend

### 2.2 Task Controller (`src/controllers/taskController.js`)
ไฟล์นี้รับผิดชอบเรื่อง "งาน" (Tasks)

**การทำงานของ `createTask`:**
1.  **รับค่า (Request Body)**: รอรับ `title`, `description`, `status`, `project_id`, `assignee_id` ฯลฯ
2.  **Validate**: (ในอนาคตควรเพิ่ม) ตรวจสอบว่า `title` ห้ามว่าง
3.  **Insert**: ใช้คำสั่ง `INSERT INTO tasks ... RETURNING *`
    *   `RETURNING *` สำคัญมาก คือบอก Database ว่าพอบันทึกเสร็จ ให้ส่งข้อมูลแถวนั้นกลับมาด้วย (จะได้ Task ID ที่เพิ่งสร้าง)
4.  **Response**: ส่งข้อมูลงานที่เพิ่งสร้างกลับไปให้ Frontend เพื่อเอาไปแสดงผลทันทีโดยไม่ต้องโหลดใหม่ทั้งหมด

---

## 3. 🎨 Frontend (หน้าบ้าน)

### 3.1 Project Details Page (`src/pages/ProjectManager/ProjectDetails.jsx`)
หน้านี้คือศูนย์กลางที่ดึงข้อมูลมาแสดง

*   **`useParams`**: ใช้ Hook นี้ดึง `projectId` จาก URL Browser
    *   *จุดที่เคยพลาด*: ตอนแรกเราตั้งชื่อตัวแปรใน App.jsx ว่า `projectId` แต่ในนี้ไปเรียกว่า `id` ทำให้ค่าเป็น undefined และโหลดไม่ขึ้น
*   **`useEffect`**: สั่งให้ทำงาน "เมื่อหน้าเว็บโหลดเสร็จ" หรือ "เมื่อ projectId เปลี่ยน" -> เรียกฟังก์ชัน `fetchProject`
*   **`fetchProject`**: ยิง API ไปหา Backend แล้วเอาข้อมูลมาเก็บใน State `project`
*   **การส่ง Props**: ส่งข้อมูล `project.members` และ `project.project_id` ต่อไปให้ Modal สร้างงาน (CreateTaskModal) เพื่อให้ Modal รู้ว่าต้องสร้างงานให้ใคร โปรเจกต์ไหน

### 3.2 Create Task Modal (`src/components/project/CreateTaskModal.jsx`)
หน้าต่างป๊อปอัพสำหรับกรอกข้อมูลงานใหม่

*   **Props**: รับ `projectId` และ `members` มาจากหน้าแม่
*   **Form State**: ใช้ `useState` เก็บค่าที่พิมพ์ (Title, Status, Assignee ฯลฯ)
*   **`handleSubmit`**:
    1.  ตรวจสอบความถูกต้อง (Validation) เช่น หัวข้อห้ามว่าง
    2.  `fetch('.../api/tasks', { method: 'POST' })`: ส่งข้อมูลไปหา Backend
    3.  **`onTaskCreated()`**: *สำคัญ* เมื่อบันทึกเสร็จ จะเรียกฟังก์ชันนี้เพื่อบอกหน้าแม่ให้ "โหลดข้อมูลใหม่เดี๋ยวนี้" ทำให้เราเห็น Task ใหม่โผล่ขึ้นมาทันทีโดยไม่ต้องกด Refresh หน้าจอเอง

---

## 4. 🔗 Database Integration
จุดเชื่อมต่อสำคัญคือไฟล์ `src/config/db.js` และคำสั่ง SQL

*   **Foreign Keys**: การเชื่อมโยงข้อมูล เช่น `tasks.project_id` เชื่อมกับ `projects.project_id` ทำให้เรารู้ว่างานนี้เป็นของโปรเจกต์ไหน
*   **JOIN**: การดึงข้อมูลข้ามตาราง เช่น ใน Task เราเก็บแค่ `assignee_id` (เลข 5) แต่ตอนโชว์ตารางงาน เราอยากได้ชื่อ "Somchai" เราจึงต้อง `JOIN users` เพื่อเอาชื่อมาแสดง

---

---

## 5. 🚀 Deep Dive: Manage Team Feature (ฟีเจอร์จัดการทีม)

ส่วนนี้อธิบายการทำงานของระบบ **"Add Member Modal"** แบบ Multi-Select ที่เพิ่งทำเสร็จ เพื่อให้เข้าใจ Flow ทั้งหมดครับ

### 5.1 Architecture Overview (ภาพรวม)
การทำงานเริ่มจาก Frontend (Modal) -> เรียก API -> Backend (Controller) -> บันทึกลง Database (Table: `projectmembers`)

### 5.2 Step-by-Step Flow

#### Step 1: Frontend - เปิด Modal & ดึงข้อมูล
เมื่อเรากดปุ่ม "Manage Team":
1.  `AddMemberModal` ถูกเปิดขึ้นมา
2.  `useEffect` ทำงานทันที:
    *   **ดึงรายชื่อ Users ทั้งหมด**: ยิง API `GET /api/users`
    *   **Filter Admin**: กรอง User ที่มี Role `Admin` ออกจากลิสต์ เพื่อไม่ให้เลือก Admin เข้าทีมได้
    *   **ตั้งค่า Default Selection**: เอา `currentMembers` (คนที่อยู่ในโปรเจกต์แล้ว) มาใส่ใน State `selectedIds` เพื่อให้มีเครื่องหมายถูก ✅ ขึ้นรอไว้เลย

#### Step 2: User Interaction (การเลือกคน)
*   **Toggle Logic**: เมื่อเราคลิกชื่อคน
    *   ถ้ามีอยู่แล้ว -> เอาออกจาก `selectedIds` (ติ๊กออก)
    *   ถ้ายังไม่มี -> ใส่เข้าไปใน `selectedIds` (ติ๊กถูก)
*   **UI Update**: สีพื้นหลังและเครื่องหมายถูกจะเปลี่ยนตาม State นี้ทันที

#### Step 3: Hitting Save (กดบันทึก)
นี่คือความฉลาดของระบบ (Smart Diffing):
เมื่อกดปุ่ม Save ระบบไม่ได้แค่ส่ง List ไปเฉยๆ แต่จะคำนวณ "ผลต่าง" (Difference):
1.  **To Add (คนใหม่)**: `selectedIds` ที่ **ไม่อยู่** ใน `originalMembers` -> ยิง API `POST /members`
2.  **To Remove (คนโดนเตะ)**: `originalMembers` ที่ **ไม่อยู่** ใน `selectedIds` -> ยิง API `DELETE /members`
*   **Result**: ระบบจะยิง API หลายตัวพร้อมกันด้วย `Promise.all` เพื่อให้ทำงานเสร็จรวดเดียว

#### Step 4: Backend Processing (`projectController.js`)
Backend รับคำสั่งและทำงานกับ Database:
*   `addProjectMember`: `INSERT INTO projectmembers (project_id, user_id) VALUES (...)`
*   `removeProjectMember`: `DELETE FROM projectmembers WHERE ...`

#### Step 5: Refreshing
เมื่อทุกอย่างเสร็จสิ้น Modal จะเรียก `onMemberAdded` (ซึ่งก็คือ `fetchProject` ของหน้าแม่) ให้ดึงข้อมูลมาแสดงใหม่ ทำให้รายชื่อที่หน้าเว็บอัปเดตทันทีโดยไม่ต้อง Refresh Browser

---
---

## 6. 🛠️ Deep Dive: Project Edit & Refactor (Updates)

ส่วนนี้อธิบายการปรับปรุงโครงสร้าง **Project Details** ที่เพิ่งทำเพิ่ม เพื่อให้รองรับการแก้ไขข้อมูลและการจัดการทีมที่ดียิ่งขึ้น

### 6.1 Edit Project Workflow
เราเปลี่ยนจากปุ่ม "Manage Team" เดิมที่เป็น Header มาเป็น **"Edit Project"**

1.  **Modal Integration**: ใช้ `EditProjectModal`
    *   **Prefill Data**: เมื่อเปิด Modal ระบบจะดึงข้อมูลปัจจุบัน (`project.name`, `status` ฯลฯ) มาใส่ใน Input ให้ทันที ทำให้ User รู้ว่ากำลังแก้ค่าอะไรอยู่
2.  **Authentication Fallback** (Backend):
    *   ใน `createProject` เราเพิ่ม Logic กันเหนียว: ปกติเราใช้ `req.user.id` (จาก Token) เป็นคนสร้าง แต่ถ้า Token หลุดหรือไม่มา เราจะดูที่ `req.body.created_by` แทน เพื่อกัน Error 500

### 6.2 Sorting Logic (การเรียงลำดับ)
User ต้องการให้โปรเจกต์ที่ **Completed** แล้ว ไปกองอยู่ล่างสุด เราจึงแก้ SQL Query:

```sql
ORDER BY 
    CASE WHEN p.project_status = 'Completed' THEN 1 ELSE 0 END, -- เอา Completed ไว้กลุ่ม 1 (ล่าง)
    p.updated_at DESC -- วันที่ล่าสุดไว้บน
```
Logic นี้เรียกว่า **Conditional Sorting**

### 6.3 UI/UX Refinements
1.  **Manage Team Button**: ย้ายไปอยู่ใน Card ของ Team Members ให้ถูกที่ถูกทาง (Contextual Action) และเปลี่ยนไอคอนเป็น `UserPlus`
2.  **Status Styling**: เพิ่มสีเหลืองสำหรับสถานะ **On Hold** เพื่อให้แยกความแตกต่างจาก In Progress (สีน้ำเงิน/ส้ม) ได้ชัดเจน

---
*เอกสารอัปเดตล่าสุด: 2025-12-11 (Project Detail Refactor)*

---

## 7. 🗑️ Deep Dive: Task Management & Deletion Logic (New)

ส่วนนี้อธิบายการทำงานของระบบลบงาน (Delete Task) ที่มีความซับซ้อนเรื่อง UI และ Backend

### 7.1 Nested Modal Strategy (React)
User Request คือต้องการหน้าต่างยืนยัน ("Are you sure?") ก่อนลบ แต่เราไม่อยากใช้ `window.confirm` ของ Browser ที่ดูไม่สวย เราจึงใช้ **Nested Modal** (Modal ซ้อน Modal)

1.  **Z-Index Layering**:
    *   `EditTaskModal` อยู่ชั้นล่าง (z-index: 1000)
    *   `ConfirmModal` อยู่ชั้นบน (z-index: 1050)
    *   เราแก้ Component `Modal.jsx` ให้รับ prop `zIndex` ได้ เพื่อให้สามารถควบคุมลำดับชั้นการซ้อนทับได้ถูกต้อง
2.  **State Management**:
    *   ใน `EditTaskModal` มี state `isDeleteConfirmOpen` เพื่อคุมการเปิด/ปิดของ Modal ตัวลูก

### 7.2 Backend Delete Logic
*   **Method**: `DELETE /api/tasks/:id`
*   **Controller**: `deleteTask`
*   **Database**: ใช้คำสั่ง `DELETE FROM tasks WHERE task_id = $1`
*   **Hard Delete**: เป็นการลบข้อมูลถาวร (ตามที่ตกลงกันใน Phase นี้) ถ้าในอนาคตต้องการเก็บประวัติอาจะต้องเปลี่ยนเป็น Soft Delete (`is_deleted = true`)

### 7.3 Avatar Fallback Logic
ใน `TeamMembers` เราเพิ่ม Logic การแสดงผลรูปโปรไฟล์:
*   เช็ค `(member.avatar)`
*   **จริง**: แสดง `<img>`
*   **เท็จ**: แสดง `<div>` สีพื้นหลัง พร้อมตัวอักษรย่อจากชื่อ (Initials) เช่น "Somchai Jaidee" -> "SJ" เพื่อความสวยงามแม้ไม่มีรูป

---

## 8. 🔍 Code Structure & Consistency Review
จากการตรวจสอบ Codebase ทั้งหมด (Frontend & Backend) พบข้อสังเกตดังนี้:

### 8.1 Architecture (โครงสร้างภาพรวม) - **Consistent (ดีมาก)**
*   **Backend**: ใช้ **MVC Pattern** อย่างเคร่งครัด (`Routes` -> `Controllers` -> `Database`).
*   **Frontend**: ใช้ **Component-based** แยก `Pages` และ `Components` ชัดเจน.

### 8.2 Styling (การตกแต่ง) - **Mixed (ผสมผสาน)**
*   **Legacy (UserManagement)**: ใช้ไฟล์ CSS แยก (`.css`).
*   **Modern (Project/Task)**: เริ่มใช้ **Tailwind CSS** เขียน class ใน JSX เลย.
*   *Note*: ในระยะยาวควรทยอยปรับ Legacy เป็น Tailwind เพื่อลดขนาดไฟล์ CSS และให้ดูแลรักษาในที่เดียวครับ.

### 8.3 Logic Patterns
*   **Async/Await**: ใช้ Modern Syntax ทั้งโปรเจกต์ (อ่านง่าย).
*   **Validation**: 
    *   User Module: เน้นความปลอดภัย (Regex Password).
    *   Project Module: เน้นความครบถ้วน (Empty Check).

---

## Kanban Board Implementation (KanbanBoardComponent, KanbanColumn, KanbanCard)

### 1. KanbanBoardComponent.jsx
ทำหน้าที่เป็นหน้าหลักของ Kanban Board (Container) ซึ่งจัดการ State ของโปรเจกต์และการลากทั้งหมด

**Key Logic:**
*   **State Management:**
    *   project: เก็บข้อมูลโปรเจกต์ทั้งหมดที่ดึงมาจาก API
    *   columns: เก็บข้อมูล Tasks โดยแยกตามสถานะ ('To Do', 'In Progress', 'In Review', 'Done')
    *   ctiveId: เก็บ ID ของ Task ที่กำลังถูกลากอยู่ ณ ขณะนั้น (เพื่อแสดง Overlay)
*   **Data Fetching:**
    *   ใช้ useEffect ดึงข้อมูลโปรเจกต์จาก API /api/projects/:id`n    *   เมื่อได้ข้อมูล Tasks มา จะทำการ Loop เพื่อจัดกลุ่มลงใน 
ewColumns ตามสถานะของ Task นั้นๆ
*   **Drag & Drop (DndContext):**
    *   ใช้ library @dnd-kit ในการจัดการ
    *   onDragStart: เซ็ต ctiveId เพื่อเริ่มแสดง Card ที่กำลังลาก
    *   onDragOver: จัดการเมื่อลากผ่าน Column อื่น (ย้าย Task ข้าม Column ระหว่างลาก)
    *   onDragEnd: จัดการเมื่อปล่อยเมาส์ (ย้าย Task ถาวรหรือจัดเรียงลำดับใหม่)

### 2. KanbanColumn.jsx
ทำหน้าที่เป็น Column แสดงรายการงานในแต่ละสถานะ (เช่น To Do List)

**Key Logic:**
*   **Droppable Zone:** ใช้ useDroppable เพื่อบอกว่าพื้นที่นี้สามารถรับ Item ที่ลากมาใส่ได้
*   **Sortable Context:** ภายใน Column ใช้ SortableContext เพื่อบอกว่ารายการข้างในสามารถจัดเรียงลำดับกันได้
*   **Mapping Tasks:** วนลูปแสดง KanbanCard ตามรายการ Tasks ที่ส่งเข้ามา

### 3. KanbanCard.jsx
ทำหน้าที่แสดงผลการ์ดงานแต่ละชิ้น (Draggable Item)

**Key Logic:**
*   **Sortable Item:** ใช้ useSortable เพื่อให้ Card นี้สามารถถูกลากและจัดเรียงได้
    *   ttributes, listeners: Props สำหรับ Accessibility และ Event Listeners การลาก
    *   	ransform, 	ransition: Style สำหรับการเคลื่อนที่ตามเมาส์
*   **UI Rendering:** แสดงผลข้อมูล Task ตาม Design
    *   **Badge:** แสดงป้าย Priority (High, Medium, Low) โดยแยกสีตามระดับความสำคัญ
    *   **Avatar:** แสดงชื่อย่อของ Assignee
    *   **Due Date:** แสดงวันที่กำหนดส่งงานรูปแบบ YYYY-MM-DD`n
