# 🛠️ Troubleshooting Log & Lessons Learned

บันทึกปัญหาที่พบระหว่างการพัฒนา (Debug Log) วิธีแก้ไข และสิ่งที่ได้เรียนรู้ เพื่อป้องกันไม่ให้เกิดซ้ำในอนาคต

---

## 1. 🔄 หน้าเว็บค้างที่ "Loading..." ไม่แสดงข้อมูล

### ❌ อาการ (Symptoms)
- หน้า `ProjectDetails` แสดงข้อความ "Loading..." ตลอดเวลา
- ไม่มีการเรียก API (Network tab ว่างเปล่า หรือเงียบกริบ)

### 🔍 สาเหตุ (Root Cause)
- **Variable Name Mismatch**:
    - ใน `App.jsx` กำหนด Route ไว้ว่่า: `/ProjectDetails/:projectId` (ใช้ชื่อ `projectId`)
    - แต่ใน `ProjectDetails.jsx` ดันไปดึงค่าด้วย: `const { id } = useParams()`
    - ผลลัพธ์คือตัวแปร `id` มีค่าเป็น `undefined`
    - เงื่อนไข `if (id) fetchProject()` จึงเป็นเท็จเสมอ ฟังก์ชันดึงข้อมูลเลยไม่เคยทำงาน

### ✅ วิธีแก้ไข (Solution)
- เปลี่ยนชื่อตัวแปรให้ตรงกันทั้งระบบ: `const { projectId } = useParams()`
- แก้ไข Dependence Array ใน useEffect เป็น `[projectId]`

---

## 2. 💥 Error 500: Internal Server Error

### ❌ อาการ (Symptoms)
- Backend ตอบกลับมาด้วย Status 500
- หน้าเว็บแสดง Error: "Failed to fetch project"

### 🔍 สาเหตุ (Root Cause)
- **Missing Database Table**:
    - Code Backend พยายาม Query ข้อมูลจากตาราง `project_members`
    - แต่ใน Database (`pgAdmin`) จริงๆ ยังไม่ได้สร้างตารางนี้
    - PostgreSQL จึงโยน Error ว่า `relation "project_members" does not exist` ทำให้ Backend Crash

### ✅ วิธีแก้ไข (Solution)
- **Workaround (ชั่วคราว)**: ใส่ `try-catch` ดักเฉพาะส่วน Query Members ไว้ เพื่อให้ Code ส่วนอื่น (Project, Tasks) ยังทำงานได้แม้ตาราง Member จะยังไม่พร้อม
- **Root Fix (ระยะยาว)**: ต้องไปสร้างตาราง `project_members` ใน Database ให้ครบตาม Schema

---

## 3. ⚠️ Unauthorized / User ID Missing

### ❌ อาการ (Symptoms)
- สร้าง Project ไม่ได้ เนื่องจากติด Error เรื่อง `created_by`

### 🔍 สาเหตุ (Root Cause)
- ระบบ Login ยังไม่ได้ทำ JWT Token เต็มรูปแบบ
- Backend พยายามเรียกใช้ `req.user.user_id` แต่ไม่มี Middleware ตัวไหนใส่ค่า `req.user` มาให้

### ✅ วิธีแก้ไข (Solution)
- เปลี่ยนไปใช้ Hardcode หรือรับ `user_id` จาก Frontend ส่งมาตรงๆ ชั่วคราว (เพื่อทดสอบการทำงานของ Feature อื่นไปก่อน)

---

## 💡 Lessons Learned (สิ่งที่เรียนรู้)
1.  **Check Console First**: เวลามีปัญหา ให้ดู Console/Network Log ใน Browser ก่อนเสมอ จะเห็น Error ชัดเจนมากกว่านั่งเดา
2.  **Naming Consistency**: การตั้งชื่อตัวแปร (เช่น `id` vs `projectId`) ต้องให้ตรงกันทั้ง Route และ Component
3.  **Defensive Coding**: การใส่ `try-catch` ดัก Error ในจุดที่เสี่ยง (เช่น Database connection) ช่วยให้ระบบไม่พังทั้งระบบเมื่อเกิดปัญหาจุดเล็กๆ
4.  **Backend Logging**: การใส่ `console.log` ใน Backend Controller ช่วยให้รู้ว่า Request วิ่งมาถึงไหนแล้ว และติดขัดตรงไหน (Debug ง่ายขึ้นมาก)

---

## 4. 👻 The "Phantom Project" Visibility Bug (Critical)

### ❌ อาการ (Symptoms)
- User ใหม่ (Sarah) เห็น Project ที่ตัวเองไม่ได้เป็นสมาชิก (เช่น Project 12)
- แต่เมื่อแก้ Code บางส่วน กลับกลายเป็นเห็น 0 Projects แทน

### 🔍 สาเหตุ (Root Cause)
1.  **Loose Filtering Query**: การใช้ `LEFT JOIN` ร่วมกับ `OR` ใน SQL Query เดิมทำให้เกิดการดึงข้อมูลผิดพลาด (Cartesian Product) เมื่อ User ไม่มี Project เลย
2.  **Incorrect ID Extraction**: Code เก่าพยายามดึง `req.user.user_id` ซึ่งเป็น `undefined` (เพราะ Frontend ส่งมาทาง Query Param `?userId=...`) ทำให้ Database ได้ค่า Null ไปค้นหา

### ✅ วิธีแก้ไข (Solution)
- **Subquery Filtering**: เปลี่ยน Logic SQL มาใช้ Subquery เพื่อความแม่นยำ:
    ```sql
    WHERE p.created_by = $1 
    OR p.project_id IN (SELECT project_id FROM projectmembers WHERE user_id = $1)
    ```
- **Correct Parameter**: เปลี่ยนกลับไปรับค่าจาก `req.query.userId` ให้ตรงกับที่ Frontend ส่งมา

---

## 5. 🧱 Z-Index & Nested Modals Conflict

### ❌ อาการ (Symptoms)
- เปิด Modal ซ้อนกัน (เช่น Delete Confirmation บน Edit Task) แล้วจอดำมืด หรือกดปุ่มผิดอัน
- Dropdown เมนูไปซ่อนอยู่หลัง Card อื่น

### 🔍 สาเหตุ (Root Cause)
- Modal ถูก Render อยู่ภายใน DOM Tree ของ Parent ทำให้ค่า `z-index` ถูกจำกัดโดย Parent (`stacking context`)

### ✅ วิธีแก้ไข (Solution)
- ใช้ **React Portals** (`createPortal`) ย้าย Modal ไป Render ที่ `document.body` โดยตรง ทำให้หลุดพ้นจากข้อจำกัดของ Parent และควบคุม Layer ได้อิสระ

---

## 6. 📋 Kanban Board & Drag-and-Drop State

### ❌ อาการ (Symptoms)
- ลากการ์ดแล้ว "เด้งกลับ" ที่เดิม (State Snapback)
- Drop แล้วข้อมูลใน Database เปลี่ยนจริง แต่หน้าจอไม่ Refresh ทันที
- Column "In Progress" ไม่ยอมรับการ์ดบางใบ

### 🔍 สาเหตุ (Root Cause)
- **Optimistic UI Failure**: เราพยายามรอ Response จาก Server ก่อนค่อยขยับการ์ด ทำให้เกิด Delay และ Animation กระตุก
- **ID Mismatch**: บางครั้ง `droppableId` เป็น String ("12") แต่ `taskId` เป็น Number (12) ทำให้ Library `dnd-kit` มองว่าเป็นคนละตัวกัน

### ✅ วิธีแก้ไข (Solution)
- **Local State Update First**: เมื่อ User ปล่อยเมาส์ ให้ขยับ UI ทันที (ไม่ต้องรอ Server) เพื่อประสบการณ์ที่ลื่นไหล
- **Strict Type Casting**: แปลง ID ทั้งหมดเป็น String ก่อนส่งให้ `dnd-kit` คำนวณ

---

## 💡 Weekly Key Takeaways (บทเรียนสำคัญประจำสัปดาห์)
1.  **Always Verify Data Source**: อย่าเชื่อ ID ที่ตาเห็นบนหน้าจอ ให้เช็ก `Network Tab` ว่าส่งไปแบบไหน (Query vs Body vs Header)
2.  **SQL Subqueries are Friends**: ในการเช็ก Permission ที่ซับซ้อน (Many-to-Many), การใช้ `IN (SELECT ...)` มักจะอ่านง่ายและแม่นยำกว่าการ `JOIN` หลายชั้นที่เสี่ยงข้อมูลเบิ้ล
3.  **Logs are Lifesavers**: การใส่ `console.log` แบบละเอียด (Verbose) ใน Controller ช่วยให้จับโจร (Bug) ได้ไวที่สุด ดีกว่านั่งเดาหน้า Frontend

---

## 7. ⚪ Blank Page / Route Error (Case Sensitivity)

### ❌ อาการ (Symptoms)
- เข้าหน้าเว็บ `/ProjectManagement` แล้วเจอหน้าขาวว่างเปล่า (Blank Page)
- Console ฟ้อง `No routes matched location`
- ลิงก์ใน Sidebar กดแล้วไม่ไปไหน หรือไปแล้วไม่เจอ

### 🔍 สาเหตุ (Root Cause)
- **Case Sensitivity Mismatch**:
    - ใน `App.jsx` ประกาศ Route เป็นตัวเล็กทั้งหมด (Kebab-case) เช่น `/project-management`
    - แต่ใน Sidebar หรือ URL Bar ผู้ใช้พยายามเข้า `/ProjectManagement` (PascalCase)
    - React Router v6 มองว่าเป็นคนละ URL กัน จึงหา Route ไม่เจอและไม่ Render อะไรเลย

### ✅ วิธีแก้ไข (Solution)
- **Consistent Naming**: ปรับลิงก์ทั้งหมดใน `Sidebar.jsx`, `Login.jsx` และการ Redirect ต่างๆ ให้เป็น **Lowercase** ทั้งหมด (e.g., `/project-management`) ให้ตรงกับที่ประกาศไว้ใน `Routes`
- ตรวจสอบ `useNavigate` ทุกจุดให้ใช้ Path ตัวเล็กเหมือนกันหมด

---

## 8. 🎨 CSS Casing & Module Not Found
### ❌ อาการ (Symptoms)
- แก้ไฟล์ `AdminDashboard.css` แล้วแต่หน้าเว็บไม่เปลี่ยนตาม
- หรือเจอ Error ว่าหาไฟล์ CSS ไม่เจอในบาง OS (Linux/Mac)

### 🔍 สาเหตุ (Root Cause)
- **Case Sensitivity**: Windows ไม่ซีเรียสเรื่องตัวเล็กตัวใหญ่ในชื่อไฟล์ (`adminDashboard.css` = `AdminDashboard.css`) แต่ระบบ Build (Vite) หรือ OS อื่นซีเรียส
- การเขียน `import` ไม่ตรงกับชื่อไฟล์จริง

### ✅ วิธีแก้ไข (Solution)
- เปลี่ยนชื่อไฟล์ให้เป็น PascalCase (`AdminDashboard.css`) ให้เหมือน React Component ไปเลยเพื่อความสม่ำเสมอ
- ตรวจสอบ path import ในไฟล์ `.jsx` ให้ตรงกันเป๊ะๆ (`../../assets/styles/AdminDashboard.css`)

---

## 9. 🔌 API 404 After Adding New Route
### ❌ อาการ (Symptoms)
- เพิ่มไฟล์ `dashboardRoutes.js` และ `app.use('/api/admin', ...)` แล้ว
- แต่พอยิง Postman หรือ Frontend เรียกใช้ กลับเจอ 404 Not Found

### 🔍 สาเหตุ (Root Cause)
- **Hot Reload Limitation**: การเพิ่ม Route ไฟล์ใหม่ใน Express บางทีกระบวนการ Hot Reload (Nodemon) อาจจะไม่ได้ reset routing caching ได้สมบูรณ์ หรือเราลืม save ไฟล์ `app.js` ที่เป็นตัวรวม Route

### ✅ วิธีแก้ไข (Solution)
- **Manual Restart**: กด `Ctrl + C` แล้วรัน `npm start` ใหม่ที่ Terminal ฝั่ง Backend เพื่อให้ Express register route ใหม่ทั้งหมดให้ชัวร์


---

## 10. 🌉 Gateway Crash: "Missing target" error

### ❌ อาการ (Symptoms)
- `gatewayServer.js` Crash ทันทีที่รัน
- Error: `[HPM] Missing "target" option. Example: {target: "http://www.example.org"}`

### 🔍 สาเหตุ (Root Cause)
- **Environment Path Resolution**: ไฟล์ `gatewayServer.js` อยู่ในโฟลเดอร์ลูก (`apiGateway/`) แต่เรียก `dotenv.config()` เฉยๆ
- library `dotenv` จะหาไฟล์ `.env` ที่ Current Working Directory ซึ่งไม่มีไฟล์นั้น ทำให้ตัวแปร `AI_SERVICE_URL` เป็น `undefined`

### ✅ วิธีแก้ไข (Solution)
- ระบุ Path ให้ชัดเจน: `dotenv.config({ path: path.join(__dirname, '../.env') });`

---

## 11. 🔌 Port Conflict Warning

### ❌ อาการ (Symptoms)
- รัน Backend (`npm start`) แล้ว Gateway (`node gatewayServer.js`) จะรันไม่ได้ (หรือสลับกัน)
- Error: `EADDRINUSE: address already in use :::5000`

### 🔍 สาเหตุ (Root Cause)
- ทั้ง 2 Service (Backend Main App & API Gateway) ถูก config ให้ใช้ Port `5000` เหมือนกันในไฟล์ `.env`

### ✅ วิธีแก้ไข (Solution)
- แยก Port ให้ชัดเจน:
    - **API Gateway**: 5000 (หน้าด่าน)
    - **Backend App**: 5001 (Microservice)

---

## 12. 🐳 Docker Network Connection Refused
### ❌ อาการ (Symptoms)
- Frontend (ใน Container) เรียก API ไม่ได้ (Connection Refused)
- Backend (ใน Container) หา Python AI Service ไม่เจอ

### 🔍 สาเหตุ (Root Cause)
- **Localhost confusion**: ใน Docker, `localhost` หมายถึง "ตัว Container เอง" ไม่ใช่เครื่องของเรา
- ถ้า Frontend เรียก `localhost:5000` มันจะหา Port 5000 ใน Container Frontend เอง ซึ่งไม่มี

### ✅ วิธีแก้ไข (Solution)
- ใช้ **Service Name** แทน IP:
    - Frontend เรียก Backend (ผ่าน Browser) -> ยังใช้ `localhost` ได้ (เพราะ Browser รันบนเครื่อง Host)
    - Gateway เรียก Backend -> ต้องใช้ `http://backend:5001` (Service Name defined in docker-compose)
    - Gateway เรียก AI Service -> ต้องใช้ `http://ai_service:8000`
