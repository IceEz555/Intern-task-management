# 🎓 คู่มือเขียน React & API ฉบับจับมือทำ
*(ฉบับเข้าใจง่าย สำหรับผู้เริ่มต้น)*

---

## 1. เริ่มเขียน Component ใหม่ยังไงดี? 🏗️

อย่าเพิ่งกังวลเรื่อง `import` ครับ ให้เริ่มจาก **"โครงสร้าง"** ก่อน

### Step 1: เขียนโครงเปล่าขึ้นมา
สร้างไฟล์ใหม่ (เช่น `MyNewPage.jsx`) แล้วพิมพ์โครงพื้นฐาน:
```javascript
// 1. สร้าง Component Function
const MyNewPage = () => {
    return (
        <div>
            <h1>สวัสดีครับ!</h1>
        </div>
    );
};

// 2. ส่งออกให้ไฟล์อื่นใช้
export default MyNewPage;
```

### Step 2: วาดหน้าตา (JSX)
ใส่สิ่งที่อยากเห็นลงไป เช่น ปุ่ม, ช่องกรอกข้อมูล
```javascript
return (
    <div>
        <h1>โปรไฟล์ของฉัน</h1>
        <input type="text" placeholder="ชื่อ..." />
        <button>บันทึก</button>
    </div>
);
```

### Step 3: เติม Logic & Import
พอเห็นหน้าตาแล้ว เราจะเริ่มรู้ว่า **"อ๋อ... ต้องใช้ตัวแปรเก็บชื่อนี่นา"** -> ค่อยกลับไป `import` ข้างบน
```javascript
import { useState } from 'react'; // <-- เพิ่มทีหลัง พิมพ์ useState แล้วกด Enter

const MyNewPage = () => {
    const [name, setName] = useState(''); // <-- เพิ่ม State
    // ...
```

---

## 2. `useState` vs `useEffect` (จำง่ายๆ) 🧠

| คำสั่ง | คืออะไร? | ใช้ตอนไหน? |
| :--- | :--- | :--- |
| **useState** | **"สมุดจด"** 📝 | เมื่อมีข้อมูลที่ **เปลี่ยนค่าได้** และต้อง **โชว์บนจอ** (เช่น สิ่งที่พิมพ์, การเปิดปิด Modal) |
| **useEffect** | **"ระบบอัตโนมัติ"** 🤖 | เมื่ออยากให้ทำอะไร **หลังจากหน้าเว็บโหลดเสร็จ** หรือ **เมื่อตัวแปรเปลี่ยน** (เช่น ดึงข้อมูล API) |

**ตัวอย่าง:**
```javascript
// 1. useState: ประกาศตัวแปร
const [count, setCount] = useState(0);

// 2. useEffect: สั่งให้ทำอะไรบางอย่างอัตโนมัติ
useEffect(() => {
    console.log("หน้านี้โหลดเสร็จแล้วนะ!");
}, []); // [] ว่างๆ แปลว่าทำแค่ครั้งแรกครั้งเดียว
```

---

---

## 3. สูตรสำเร็จการเรียก API (4 Steps) 🌐

เวลาจะคุยกับ Backend ให้คิดเป็นสเต็ปแบบนี้เสมอครับ:

### Step 1: Trigger (ใครสั่ง?) 🔫
จะให้ทำงานตอนไหน?
*   เปิดหน้าเว็บปุ๊บ? -> เขียนใน `useEffect`
*   กดปุ่ม? -> เขียนในฟังก์ชัน `handleClick`

### Step 2: Collect Data (เตรียมของ) 🎒
จะส่งอะไรไปบ้าง? เก็บใส่ตัวแปรไว้
```javascript
const payload = {
    username: "admin",
    role: "manager" // ข้อมูลพวกนี้มักมาจาก State
};
```

### Step 3: Action (ส่งของ) 🚚
ใช้ `axios` ยิงไปที่ Backend (ผ่าน Gateway)
*   **Tip**: ควรใช้ `VITE_API_URL` หรือไฟล์ `api.js` ที่ตั้งค่าไว้แล้ว
    
```javascript
import api from '../utils/api'; // ตัวอย่างการ import instance

// ...
await api.get('/users'); 
// หรือถ้าใช้ axios ตรงๆ:
// await axios.get(`${import.meta.env.VITE_API_URL}/api/users`);
```

*   **GET** (ขอดู): `api.get('/users')`
*   **POST** (สร้าง): `api.post('/users', payload)`
*   **PUT** (แก้ไข): `api.put('/users/1', payload)`
*   **DELETE** (ลบ): `api.delete('/users/1')`

**อย่าลืม!** ต้องมีคำว่า `await` เสมอ เพราะเน็ตอาจจะช้า เราต้องรอ
```javascript
await axios.post('...', payload);
```

### Step 4: Aftermath (จบงาน) 🎉
ทำเสร็จแล้ว เอาไงต่อ?
*   แจ้งเตือน: `alert("สำเร็จ!")`
*   ปิด Modal: `onClose()`
*   โหลดใหม่: `fetchData()`

---

---

## 4. Async / Await คืออะไร? ⏳

คือการบอกโปรแกรมว่า **"ใจเย็นๆ รอแป๊บบบนึง"**

```javascript
// ❌ แบบผิด (ไม่รอ)
const submit = () => {
    axios.save(data); // โปรแกรมสั่งปุ๊บ วิ่งไปบรรทัดต่อไปเลย ทั้งที่ยัง save ไม่เสร็จ
    alert("เสร็จแล้ว"); // เด้งขึ้นมาทั้งที่จริงๆ ยังไม่เสร็จ (User งง)
}

// ✅ แบบถูก (ใส่ async/await)
const submit = async () => {  // 1. บอกว่าฟังก์ชันนี้มีการรอนะ
    await axios.save(data);   // 2. รอจนกว่าจะ save เสร็จจริงๆ
    alert("เสร็จแล้ว");       // 3. ค่อยเด้งเตือน
}
```

---

## 5. Logic เปิด-ปิด Modal (สวิตช์ไฟ) 💡

คิดซะว่า Modal คือหลอดไฟครับ

1.  **ตัวแม่ (Parent)**: ถือสวิตช์ (`isOpen = true/false`)
2.  **ตัวลูก (Modal)**: รับค่าไปดู (`props.open`)

**Flow การทำงาน:**
1.  แม่กดปุ่ม -> สั่ง `setIsOpen(true)` -> ส่งค่า `true` ไปให้ลูก -> **ลูกโชว์ตัว**
2.  ลูกกดกากบาท -> ลูกตะโกนบอกแม่ (`props.onClose`) -> แม่สั่ง `setIsOpen(false)` -> **ลูกหายตัว**

---

*หวังว่าคู่มือนี้จะช่วยให้เข้าใจภาพรวมชัดขึ้นนะครับ! ลุยต่อได้เลย! 🚀*
